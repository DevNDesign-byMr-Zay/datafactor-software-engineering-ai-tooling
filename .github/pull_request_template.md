## Objective
Describe the smallest problem this PR solves and why it belongs in the maintained code path.

## Scope
- What changed:
- What was intentionally left unchanged:
- Related issue/PR:

## Evidence and trust boundary
- Producer of the evidence or decision:
- Fields/identity this PR trusts:
- Fields/inputs this PR rejects or treats as diagnostic-only:
- Authority explicitly **not** added by this PR:

## Validation
- [ ] Focused regression covers the bug/contract gap.
- [ ] Legitimate current input remains accepted.
- [ ] Engineering CI passes on the exact head.
- [ ] CodeQL/security gate passes on the exact head.
- [ ] No secrets, credentials, raw environment dumps, or provider noise enter durable evidence.
- [ ] No quality/security threshold was lowered to make the change pass.

## Reviewer attack surface
List the malformed, replayed, accessor-backed, symbolic, inherited, stale, or cross-artifact cases reviewers should try before approval.

## Merge note
If this PR is stacked, state the required merge order and retarget/retest plan. Otherwise write `not stacked`.
